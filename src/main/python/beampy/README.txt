If you use a 32-bit Python, also a 32-bit Java VM must be used.

Before the Python interpreter is started, the PATH to the correct JVM must be set.
E.g. on Windows:
    set PATH=%JDK32_HOME%\jdk1.7.0_07\jre\bin;%JDK32_HOME%\jre\bin\server;%PATH%
